name: Django CI/CD Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: django-production-runner
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    env:
      DB_HOST: ${{ secrets.DB_HOST }}
      DB_NAME: ${{ secrets.DB_NAME }}
      DB_USER: ${{ secrets.DB_USER }}
      DB_PASS: ${{ secrets.DB_PASS }}
      DB_PORT: ${{ secrets.DB_PORT }}
      DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY }}
      DJANGO_SALT_KEY: ${{ secrets.DJANGO_SALT_KEY }}
      CLOUDFLARE_TOKEN: ${{ secrets.CLOUDFLARE_TOKEN }}
      SUPERUSER_NAME: ${{ secrets.SUPERUSER_NAME }}
      SUPERUSER_EMAIL: ${{ secrets.SUPERUSER_EMAIL }}
      SUPERUSER_PASSWORD: ${{ secrets.SUPERUSER_PASSWORD }}
      ALLOWED_HOSTS: ${{ secrets.ALLOWED_HOSTS }}
      CSRF_TRUSTED_ORIGINS: ${{ secrets.CSRF_TRUSTED_ORIGINS }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install pips
        run: |
          python -m pip install --upgrade pip
          pip install pymysql gunicorn "django-import-export>=3.3.0" "django>=5.2,<5.3" django-dbbackup django-axes reportlab openpyxl pillow python-barcode qrcode cryptography requests django-pwa

      - name: Diagnose Database
        run: |
          DIAG_RESULT=$(python -c "
          import time, pymysql, os
          start = time.time()
          try:
              conn = pymysql.connect(
                  host=os.environ['DB_HOST'],
                  user=os.environ['DB_USER'],
                  password=os.environ['DB_PASS'],
                  database=os.environ['DB_NAME'],
                  port=int(os.environ['DB_PORT']),
                  ssl={'ssl-mode': 'REQUIRED'}
              )
              cursor = conn.cursor()
              cursor.execute('SELECT VERSION();')
              db_version = cursor.fetchone()
              end = time.time()
              ms = round((end - start)*1000, 2)
              print(f'SUCCESS: Connected to Aiven MySQL in {ms} ms')
              conn.close()
          except Exception as e:
              print(f'ERROR: Database connection failed: {e}')
          ")
          echo "$DIAG_RESULT"
          curl -s -d "DB Diagnosis Logs: $DIAG_RESULT" ntfy.sh/hshshehgag || true

      - name: Run Database Migrations
        run: |
          python manage.py makemigrations
          python manage.py migrate

      - name: Create Default Superuser
        run: |
          python manage.py shell -c "
          import os
          from django.contrib.auth.models import User
          username = os.environ.get('SUPERUSER_NAME')
          email = os.environ.get('SUPERUSER_EMAIL')
          password = os.environ.get('SUPERUSER_PASSWORD')
          if username and password:
              if not User.objects.filter(username=username).exists():
                  User.objects.create_superuser(username, email, password)
                  print('Superuser created successfully.')
              else:
                  print('Superuser already exists.')
          else:
              print('Skipping superuser creation: Credentials not provided in secrets.')
          "

      - name: Process & Place PWA Icons from Project Root
        run: |
          python -c "
          import os
          from PIL import Image

          project_dir = os.getcwd()
          target_dir = os.path.join(project_dir, 'ceo_module', 'static', 'ceo', 'images')
          os.makedirs(target_dir, exist_ok=True)

          extensions = ('.png', '.jpg', '.jpeg', '.webp')
          found_images = [
              os.path.join(project_dir, f) for f in os.listdir(project_dir)
              if f.lower().endswith(extensions)
          ]

          if not found_images:
              print('No source image found in Project Root directory to resize.')
          else:
              source_img_path = found_images[0]
              print(f'Processing source image: {source_img_path}')

              sizes = [(192, 192, 'icon-192.png'), (512, 512, 'icon-512.png')]

              with Image.open(source_img_path) as img:
                  img = img.convert('RGBA')
                  for width, height, filename in sizes:
                      resized_img = img.resize((width, height), Image.Resampling.LANCZOS)
                      output_path = os.path.join(target_dir, filename)
                      resized_img.save(output_path, 'PNG')
                      print(f'Saved: {output_path}')
          "

      - name: Collect Static Files
        run: |
          python manage.py collectstatic --noinput

      - name: Install Cloudflare Tunnel (cloudflared)
        run: |
          curl -L --output cloudflared https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
          chmod +x cloudflared
          sudo mv cloudflared /usr/local/bin/cloudflared

      - name: Check and Kill Existing Local Processes
        run: |
          echo "Cleaning up any old Django or Cloudflared instances on the runner..."
          pkill -f "manage.py runserver" || true
          pkill -f "cloudflared" || true
          sleep 2

      - name: Start Django Server & Verify Health
        run: |
          echo "Starting Django in background using nohup..."
          nohup python manage.py runserver 0.0.0.0:8000 > django.log 2>&1 &
          
          echo "Waiting for Django to bind on port 8000..."
          sleep 5
          
          # Verification check ki server sach me response de raha hai ya crash hua
          if curl -s http://localhost:8000 > /dev/null; then
            echo "SUCCESS: Django local server is UP and responding on port 8000!"
          else
            echo "ERROR: Django server failed to start on port 8000! Printing django.log:"
            cat django.log
            exit 1
          fi

      - name: Start Cloudflare Tunnel
        run: |
          echo "Starting Cloudflare Tunnel in background..."
          nohup cloudflared tunnel run --token ${{ secrets.CLOUDFLARE_TOKEN }} > tunnel.log 2>&1 &
          
          sleep 5
          echo "=== TUNNEL ACTIVE LOGS ==="
          cat tunnel.log

      - name: Get Public IP & Send Notification to ntfy
        run: |
          PUBLIC_IP=$(curl -s ifconfig.me || curl -s api.ipify.org)
          echo "GitHub Runner Public IP: $PUBLIC_IP"
          
          MESSAGE="Django App Live! 🚀
          Domain: https://shops.hadi88.online
          Direct IP: http://$PUBLIC_IP:8000"
          
          curl -s -d "$MESSAGE" ntfy.sh/hshshehgag || true

      - name: Hold workflow for 3 hours & monitor
        run: |
          echo "Workflow is now running for 3 hours. Your app is live."
          
          for i in {1..36}; do
            sleep 300
            echo "--- Monitoring Check ($i/36) ---"
            if ! curl -s http://localhost:8000 > /dev/null; then
               echo "ALERT: Django server down! Logs below:"
               cat django.log
            else
               echo "Django Server Status: HEALTHY"
            fi
          done
